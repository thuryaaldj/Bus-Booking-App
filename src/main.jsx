import React from "react";
import ReactDOM from "react-dom/client";

import MainWithTheme from "./MainWithTheme";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <MainWithTheme />
  </React.StrictMode>
);
